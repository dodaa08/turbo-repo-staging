name : Deploy to prod
on : 
  push:
     branches: [ new_main ]
      
jobs:
  redeploy_everything:
     runs-on: ubuntu-latest
     name: Deploying all
     steps:
      - run: |
          # Create and secure the private key file
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/SSH_KEY
          chmod 600 ~/SSH_KEY
          
          # Connect via SSH and run all commands on the remote server
          ssh -i ~/SSH_KEY -o StrictHostKeyChecking=no ubuntu@${{ secrets.EC2_HOST }} << 'EOF'
            cd turbo-repo-staging
            git pull origin new_main  # <-- Make sure this matches your push branch
            pnpm install
            pnpm run build
            pm2 restart all
          EOF

