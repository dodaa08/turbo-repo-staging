version: '3.8'

services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.be
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=${PORT}
    env_file:
      - ../.env
    restart: unless-stopped

